model
  schema 1.1

type user

type group
  relations
    define member: [user]

type document
  relations
    define admin: [user]
    define reader: [user, group#member]
    define writer: [user]

    define edit: writer
    define view: reader or edit
